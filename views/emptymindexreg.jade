doctype html(lang='en')
head
    meta(charset='utf-8')
    |     
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    |     
    meta(name='viewport', content='width=device-width, initial-scale=1')
    |     
    title P&B
    // Bootstrap
    link(href='/bootstrap/css/bootstrap.min.css', rel='stylesheet')
    // HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries
    // WARNING: Respond.js doesn't work if you view the page via file://
    //if lt IE 9
      script(src='https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js')
      |       
      script(src='https://oss.maxcdn.com/respond/1.4.2/respond.min.js')     
    style.
      @font-face {
      font-family:roboto-thin;
      src:url('/bootstrap/fonts/Roboto-Thin.ttf');
      }
      @font-face {
      font-family:high;
      src: url('/bootstrap/fonts/Attentica.ttf');
      }
      body {
        font-family:roboto-thin;
      }
      h1 {
        font-family:roboto-thin;
        color:white;
      }
      h2 {
        font-family:roboto-thin;
        color:white;
      }
      h3 {
        font-family:roboto-thin;
        color:white;
      }
      h4 {
        font-family:roboto-thin;
        color:white;
      }
      h5 {
        font-family:roboto-thin;
        color:white;
      }
      h6 {
        font-family:roboto-thin;
        color:white;
      }
      textarea:hover, 
      input:hover, 
      textarea:active, 
      input:active, 
      textarea:focus, 
      input:focus,
      button:focus,
      button:active,
      button:hover
      {
          outline:0px !important;
          -webkit-appearance:none;
      }
      nav ul li a {
        border:0px !important;
        background-color:rgba(255, 255, 255, 0.26) !important;
        color:white !important;
      }
      .navbar-inverse {
      background-color: rgba(34, 34, 34, 0.34);
      border:none;
      }
      .navbar-form{
        margin-left:0px;
        margin-right:0px;
        bottom:30px;
        position:absolute;
      }
      .cbp-spmenu {
        background: #1d1d1d;
        position: fixed;
      }
      
      .cbp-spmenu h3 {
        color: indianred;
        font-size: 1.9em;
        padding: 20px;
        padding-top:27px;
        margin: 0;
        font-weight: 300;
      }
      
      .cbp-spmenu a {
        display: block;
        color: #ddd;
        font-size: 20px;
        font-weight: 300;
      }
      
      .cbp-spmenu a:hover {
        background: #333;
        text-decoration:none !important;
      }
      
      .cbp-spmenu a:active {
        background: #afdefa;
        color: #47a3da;
      }
      .cbp-spmenu-vertical {
        width: 240px;
        margin-left:74px;
        border-left:1px solid #333;
        height: 100%;
        top: 0;
        z-index: 1000;
      }
      
      .cbp-spmenu-vertical a {
        padding: 1em;
      }
      .cbp-spmenu-left {
        left: -315px;
      }
      .cbp-spmenu-left.cbp-spmenu-open {
        left: -74px;
      }
      .cbp-spmenu-push {
        overflow-x: hidden;
        position: relative;
        left: 0;
      }
      
      .cbp-spmenu-push-toright {
        left: 240px;
      }
      
      .cbp-spmenu-push-toleft {
        left: -240px;
      }
      .cbp-spmenu,
      .cbp-spmenu-push {
        -webkit-transition: all 0.3s ease;
        -moz-transition: all 0.3s ease;
        transition: all 0.3s ease;
      }
      .container {
        transition: 0.5s -webkit-filter linear;
        transition: background-color 0.5s linear;
      }
      .navbar-right {
        float: none !important; 
      }
      footer {
          position: fixed;
          bottom: 0;
          width: 100%;
          background-color:#333;
      }
      input[type="text"], textarea {
      outline: none;
      box-shadow:none !important;
      }
      .datecl {
        color:#333;
      }
      .yet{
       color:white;
      }
      .alreadyb{
      color:rgba(0, 0, 0, 0.27);
      }
      .alreadym{
      color:rgba(255, 255, 255, 0.21);
      }
      {
        padding-left:30px;
      }
      textarea:hover, 
      input:hover, 
      textarea:active, 
      input:active, 
      textarea:focus, 
      input:focus,
      button:focus,
      button:active,
      button:hover
      {
          outline:0px !important;
          -webkit-appearance:none;
      }
      #addbook{
        background-color: #A9A9A9;
      }
      .activebutton {
        background-color:#252525;
      }
      .h1 .small,
       .h1 small,
       .h2 .small,
       .h2 small,
       .h3 .small,
       .h3 small,
       .h4 .small,
       .h4 small,
       .h5 .small,
       .h5 small,
       .h6 .small,
       .h6 small,
       h1 .small,
       h1 small,
       h2 .small,
       h2 small,
       h3 .small,
       h3 small,
       h4 .small,
       h4 small,
       h5 .small,
       h5 small,
       h6 .small,
       h6 small {
      color: rgba(245, 245, 245, 0.68);
      }
      .modpanel .btn{
        margin:10px;
        border-radius:30px;
      }
  body
    nav(class="cbp-spmenu cbp-spmenu-vertical cbp-spmenu-left" id="cbp-spmenu-s1")
      h3(style='text-align:center;') People & Books
      // 
        form(class="navbar-form navbar-right" method='post' action='/srch' id='srch' role="search")
          .input-group
             input(type="text" class="form-control" name='searchfield' id='searchfield' style='background-color:#333;color:white;border-color:#333;border-top-left-radius:30px;border-bottom-left-radius:30px;') 
             span(class='input-group-btn')
               button(class="btn btn-default" type="submit" id='searchbutton' style='padding-top:9px;padding-bottom:9px;background-color:#333;color:gray;border-color:#333;border-top-right-radius:30px;border-bottom-right-radius:30px;')
                 span(class='glyphicon glyphicon-search') 
      a(href="http://m.peopleandbooks.com/settings") Настройки
      a(href='http://m.peopleandbooks.com/about') О проекте
      a(href="http://peopleandbooks.com/logout") Выйти
    .container( id='maincont' style='background-color:#52CA8F;overflow:auto;height:100%;padding-left:20px;padding-right:0px;padding-bottom:74px;')
      #booksrow.row
        #bookinsert.col-xs-12
          .row
            .col-xs-12(style='text-align:center;')
              h1(id='booksfigures' style='font-family:high;font-size:100px;font-weight:bold;color:white;padding-right:20px;')
          #welcomeb.row(style='text-align:center;padding-right:20px;')
            .col-xs-12
              h2 Welcome !
              h3 Add a book by touching + at the bottom of the screen
          .row
            #bookinsert.col-xs-12
              //add a smile here or something
      #moviesrow.row(style='display:none;')
        .col-xs-12
          .row
            .col-xs-12(style='text-align:center;')
              h1(id='moviesfigures' style='font-family:high;font-size:100px;font-weight:bold;color:white;padding-right:20px;')
          #welcomem.row(style='text-align:center;padding-right:20px;')
            .col-xs-12
              h2 Welcome !
              h3 Add a book by touching + at the bottom of the screen
          .row
            #movieinsert.col-xs-12
          //add a smile here or something
    footer(class='footer')
      .container
        .row(id='addbook' style='display:none;padding-top:20px;background-color:#378961;')
          .col-xs-12
            .form-group
                label(for="exampleInputEmail1") Название
                input(type="email" class="form-control" id="booktitle" placeholder="Crime and punishment")
            .form-group
              label(for="exampleInputEmail1") Автор
              input(type="email" class="form-control" id="author" placeholder="Dostoevsky F.M.")
            .checkbox
              label
                input(type="checkbox" id='newbook') 
                | Уже прочитал
            .checkbox
              label
                input(type="checkbox" id='star') 
                span(class='glyphicon glyphicon-star' style='color:#F2D45C;margin-right:10px;')
                | Книга понравилась
            .checkbox
              label
                input(type="checkbox" id='attention') 
                span(class='glyphicon glyphicon-exclamation-sign' style='color:rgba(255, 255, 255, 0.65);margin-right:10px;')
                | Прочитать скорее
            nav
              ul(class="pager")
                li(class="previous")
                  a(style='background-color:#EFAF4F;' id='booksubmitbutton') Add
                li(class="next")
                  a( class='addbookbutton' style='background-color:#999;') Close
        .row(id='addmovie' style='display:none;padding-top:20px;background-color:#A03F39;')
          .col-xs-12
            .form-group
              label(for="exampleInputEmail1") Название
              input(type="email" class="form-control" id="movietitle" placeholder="No country for old men")
            .form-group
              label(for="exampleInputEmail1") Год
              input(type="email" class="form-control" id="year" placeholder="2007")
            .checkbox
              label
                input(type="checkbox" id='newmovie') 
                | Уже посмотрел
            .checkbox
              label
                input(type="checkbox" id='moviestar') 
                span(class='glyphicon glyphicon-star' style='color:#F2D45C;margin-right:10px;')
                | Фильм понравился
            .checkbox
              label
                input(type="checkbox" id='movieattention') 
                span(class='glyphicon glyphicon-exclamation-sign' style='color:rgba(255, 255, 255, 0.65);margin-right:10px;')
                | Посмотреть скорее
            nav
              ul(class="pager")
                li(class="previous")
                  a(style='background-color:#EFAF4F;' id='moviesubmitbutton') Add
                li(class="next")
                  a( class='addbookbutton' style='background-color:#999;') Close
        .row(style='text-align:center;')
          .col-xs-3.addbookbutton
            h1( style='color:white;font-size:40px;padding-top:10px;padding-bottom:10px;margin-top:0px;margin-bottom:0px;') +
          .col-xs-3.activebutton( id='showbooks')
            h1(style='color:white;font-size:40px;padding-top:10px;padding-bottom:10px;margin-top:0px;margin-bottom:0px;') B
          .col-xs-3(id='showmovies')
            h1(style='color:white;font-size:40px;padding-top:10px;padding-bottom:10px;margin-top:0px;margin-bottom:0px;') M
          .col-xs-3
            span(class='glyphicon glyphicon-align-justify ' id='showLeft' style='color:white;font-size:30px;padding-top:13px;')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js')
    script(src='/js/bootstrap.min.js')
    script.
      // Setting user books/movies counters
        var seenmovies = #{seenmovies};
        var newmovies = #{newmovies};
        $('#moviesfigures').append(" "+newmovies+" <span class='alreadym'> | "+seenmovies+"</span>");
        var readbooks = #{readbooks};
        var newbooks = #{newbooks};
        $('#booksfigures').append(" "+newbooks+" <span class='alreadyb'> | "+readbooks+"</span>");
        //End of setting counters
      // Function for opening panel with remove/modify buttons
        function openmodpanel(bid) {
         eval("$('#modbook"+bid+"').slideToggle();");
        }
        //end of openmodpanel funciton
        // Function for opening panel with remove/modify buttons in movies column
        function openmodmoviepanel(mid) {
         eval("$('#modmovie"+mid+"').slideToggle();");
        }
        //end of openmodpanel funciton for movies column
      // CA5B52 for movies 99362F
      $('.addbookbutton').click(function(){
          if($('#showbooks').hasClass('activebutton'))
          {$('#addbook').slideToggle();}
          else
            {$('#addmovie').slideToggle();}
          });
      $('#shownav').click(function(){
          $('#shownav').fadeOut();
          $('#nav').fadeIn();
          });
      $('#showmovies').click(function(){
         $('#booksrow').fadeOut();
         $('#maincont').css('background-color','#CA5B52');
         $('#moviesrow').fadeIn();
         $('.activebutton').removeClass('activebutton');
         $('#showmovies').addClass('activebutton');
        });
      $('#showbooks').click(function(){
         $('#moviesrow').fadeOut();
         $('#maincont').css('background-color','#52CA8F');
         $('#booksrow').fadeIn();
         $('.activebutton').removeClass('activebutton');
         $('#showbooks').addClass('activebutton');
        });
        $('#login').click(authsubmit);
      var counter =0;
        var menuLeft = document.getElementById( 'cbp-spmenu-s1' ),
            menuRight = document.getElementById( 'cbp-spmenu-s2' ),
            showLeft = document.getElementById( 'showLeft' ),
            showRight = document.getElementById( 'showRight' ),
            body = document.body;
        
        showLeft.onclick = function() {
          switch(counter)
            {case(1):
                         counter=0;
                         $('#showLeft').removeClass('active' );
                         $('#cbp-spmenu-s1').removeClass('cbp-spmenu-open' );
                         $('.container').css('-webkit-filter','brightness(1)');
                        break;
                        case(0):
                          counter=1;
                          $('#showLeft').addClass('active' );
                          $('#cbp-spmenu-s1').addClass('cbp-spmenu-open' );
                          $('.container').css('-webkit-filter','brightness(0.6)');
                        break;
                      }
        };
      // Book submit mechanics
        var allowsubmit = 1;
        function booksubmit() {
        if(!allowsubmit) {
          alert('Submit is in progress');
          return
        }
        var author = $('#author').val();
        if(!author){
          author='--';
        }
        var booktitle = $('#booktitle').val();
        if(!booktitle) {
          alert('Specify book title');
          allowsubmit=1;
          return
        }
        var uid = #{uid};
        var star;
        var attention;
        var newbook;
        if($('#star').is(':checked')){
          star = 1;
        }
        else {
          star=0;
        }
        if($('#attention').is(':checked')){
          attention = 1;
        }
        else {
          attention=0;
        }
        if($('#newbook').is(':checked')){
          newbook = 0;
        }
        else {
          newbook=1;
        }
        allowsubmit = 0;
        request = new XMLHttpRequest();
        request.onreadystatechange = function () {
        var DONE = this.DONE || 4;
        if (this.readyState === DONE){
        var index;
        parsed = JSON.parse(request.responseText);
        console.log(parsed);
        if(parsed.trouble === 1)
        {
        console.log('TROUBLE === 1');
        alert('Server is down, try again later.')
        allowsubmit=1;
        }
        else {
          var bid = parsed.bid;
          $('#addbook').slideToggle();
         $('#author').val('');
          $('#booktitle').val('');
          $("#checkbox").prop("newbook", false);
          $("#checkbox").prop("star", false);
          $("#checkbox").prop("attention", false);
        var bookclass = 'yet';
        if(newbook===0)
        {
         bookclass='alreadyb';
        }
        var visibleinsert = "<div class='row' onclick='openmodpanel("+bid+")' id='book"+bid+"'><div class='col-xs-12'><h4 style='margin-left:40px;' class='"+bookclass+"'>"+booktitle+" <small> "+author+" </small>";
               if(star===1&&newbook===0){
                 visibleinsert+="<span style='color:#F2D45C;margin-left:10px;' class='glyphicon glyphicon-star'></span>";
               }
               if(attention===1&&newbook===1){
                 visibleinsert+="<span style='color:rgba(255, 255, 255, 0.65);margin-left:10px;' class='glyphicon glyphicon-exclamation-sign'></span>";
               }
               visibleinsert+="</h4></div></div>";
               var hiddeninsert="<div class='row modpanel' id='modbook"+bid+"' style='text-align:center;display:none;background-color:#333;'><div class='col-xs-12'>";
               if(newbook){
                hiddeninsert+="<button class='btn btn-primary' id='readbutton"+mid+"' onclick='modify(0,"+bid+")' type='button'>Прочитана</button>";
               }
               hiddeninsert+="<button class='btn btn-danger' onclick='modify(1,"+bid+")' type='button'>Удалить</button></div</div>";
               //$('#bookinsert').prepend(insert);
        //$('#welcome').fadeOut();
        //$('#booksrow').slideToggle();
        if(newbook===1)
        {newbooks++;
        $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadyb'> | "+readbooks+"</span>");
        }
        else {
          readbooks++;
          $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadyb'> | "+readbooks+"</span>");
        }
        $(hiddeninsert).prependTo('#bookinsert');
        $(visibleinsert).prependTo('#bookinsert').hide().slideDown();
        allowsubmit=1;
        //window.location = 'http://m.peopleandbooks.com';
        }
        }
        };
        request.open('POST', '/addbook', true);
        request.setRequestHeader('X-Requested-With', 'XMLHttpRequest');  // Tells server that this call is made for ajax purposes.
        request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        //Most libraries like jQuery/Prototype/Dojo do this
        console.log("author="+author+"&booktitle="+booktitle+"&star="+star+"&attention="+attention+"&uid="+uid+"&newbook="+newbook);
        request.send("author="+author+"&booktitle="+booktitle+"&star="+star+"&attention="+attention+"&uid="+uid+"&newbook="+newbook);
        }
        //End of book submit mechanics
      
      $('#booksubmitbutton').click(booksubmit);
      // Function for movie submition 
        function moviesubmit() {
        if(!allowsubmit) {
          return
        }
        var year = $('#year').val();
        if(!year){
          year='--';
        }
        var movietitle = $('#movietitle').val();
        if(!movietitle) {
          alert('Specify movie title');
          allowsubmit=1;
          return
        }
        var uid = #{uid};
        var moviestar;
        var attention;
        var newmovie;
        if($('#moviestar').is(':checked')){
          moviestar = 1;
        }
        else {
          moviestar=0;
        }
        if($('#movieattention').is(':checked')){
          movieattention = 1;
        }
        else {
          movieattention=0;
        }
        if($('#newmovie').is(':checked')){
          newmovie = 0;
        }
        else {
          newmovie=1;
        }
        allowsubmit = 0;
        request = new XMLHttpRequest();
        request.onreadystatechange = function () {
        var DONE = this.DONE || 4;
        if (this.readyState === DONE){
        var index;
        parsed = JSON.parse(request.responseText);
        console.log(parsed);
        if(parsed.trouble === 1)
        {
        console.log('TROUBLE === 1');
        alert('Server is down, try again later.')
        allowsubmit=1;
        }
        else {
          var mid = parsed.mid;
          $('#addmovie').slideToggle();
         $('#year').val('');
          $('#movietitle').val('');
          $("#checkbox").prop("newmovie", false);
          $("#checkbox").prop("moviestar", false);
          $("#checkbox").prop("movieattention", false);
        var movieclass = 'yet';
        if(newmovie===0)
        {
         movieclass='alreadym';
        }
        var visibleinsert = "<div class='row' onclick='openmodpanel("+mid+")' id='movie"+mid+"'><div class='col-xs-12'><h4 style='margin-left:40px;' class='"+movieclass+"'>"+movietitle+" <small> "+year+" </small>";
               if(moviestar===1&&newmovie===0){
                 visibleinsert+="<span style='color:#F2D45C;margin-left:10px;' class='glyphicon glyphicon-moviestar'></span>";
               }
               if(movieattention===1&&newmovie===1){
                 visibleinsert+="<span style='color:rgba(255, 255, 255, 0.65);margin-left:10px;' class='glyphicon glyphicon-exclamation-sign'></span>";
               }
                visibleinsert+="</h4></div></div>";
                var hiddeninsert =" <div class='row modpanel' id='modmovie"+mid+"' style='text-align:center;display:none;background-color:#333;'><div class='col-xs-12'>";
               if(newmovie){
                hiddeninsert+="<button class='btn btn-primary' id='watchbutton"+mid+"' onclick='modifymovie(0,"+mid+")' type='button'>Просмотрен</button>";
               }
               hiddeninsert+="<button class='btn btn-danger' onclick='modifymovie(1,"+mid+")' type='button'>Удалить</button></div</div>";
               //$('#movieinsert').prepend(insert);
        //$('#welcome').fadeOut();
        //$('#moviesrow').slideToggle();
        if(newmovie===1)
        {newmovies++;
        $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadym'> | "+seenmovies+"</span>");
        }
        else {
          seenmovies++;
          $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadym'> | "+seenmovies+"</span>");
        }
        $(hiddeninsert).prependTo('#movieinsert');
        $(visibleinsert).prependTo('#movieinsert').hide().slideDown();
        allowsubmit=1;
        //window.location = 'http://m.peopleandmovies.com';
        }
        }
        };
        request.open('POST', '/addmovie', true);
        request.setRequestHeader('X-Requested-With', 'XMLHttpRequest');  // Tells server that this call is made for ajax purposes.
        request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        //Most libraries like jQuery/Prototype/Dojo do this
        console.log("year="+year+"&movietitle="+movietitle+"&moviestar="+moviestar+"&movieattention="+movieattention+"&uid="+uid+"&newmovie="+newmovie);
        request.send("year="+year+"&movietitle="+movietitle+"&moviestar="+moviestar+"&movieattention="+movieattention+"&uid="+uid+"&newmovie="+newmovie);
        }
        //End of movie submit mechanics
        $('#moviesubmitbutton').click(moviesubmit);
        // Function for removing or changing the status of the movie
        var allowmod = 1;
        function modifymovie(rem,mid) {
         if (!allowmod) {
          return;
         }
        allowmod = 0;
        request = new XMLHttpRequest();
        request.onreadystatechange = function () {
        var DONE = this.DONE || 4;
        if (this.readyState === DONE){
        var index;
        parsed = JSON.parse(request.responseText);
        console.log(parsed);
        if(parsed.trouble === 1)
        {
        console.log('TROUBLE === 1');
        switch (parsed.mtext){
        case('db'):
        alert('Проблема с базой данных, повторите позднее');
        allowmod = 1;
        break;
        }}
        else {
          var moviestatus = parseInt(parsed.moviestatus);
          console.log('moviestatus:'+moviestatus);
         if(rem) {
           if (eval("$('#modmovie"+mid+"').css('display')") === 'block' )
         {eval("$('#modmovie"+mid+"').slideToggle('400',function(){$('#modmovie"+mid+"').remove();})");} 
          eval("$('#movie"+mid+"').fadeOut('400',function(){$('#movie"+mid+"').remove();});");
          if(moviestatus===1)
        {newmovies--;
        $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadym'> | "+seenmovies+"</span>");
        }
        else {
          seenmovies--;
          $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadym'> | "+seenmovies+"</span>");
        }
         }
         else {
          if (eval("$('#modmovie"+mid+"').css('display')") === 'block' )
         {eval("$('#modmovie"+mid+"').slideToggle();");}
           eval("$('#watchbutton"+mid+"').remove();");
           eval("$('#heading"+mid+"').removeClass('yet').addClass('alreadym');");
           newmovies--;
           seenmovies++;
         $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadym'> | "+seenmovies+"</span>");
         }
         allowmod=1;
        }
        }
        };
        request.open('POST', '/modmovie', true);
        request.setRequestHeader('X-Requested-With', 'XMLHttpRequest');  // Tells server that this call is made for ajax purposes.
        request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        //Most libraries like jQuery/Prototype/Dojo do this
        request.send("rem="+rem+"&uid=#{uid}&mid="+mid);
        }
        //End of remove/change status of the movie function
        // Function for removing or changing the status of the book
        var allowmod = 1;
        function modify(rem,bid) {
         if (!allowmod) {
          return;
         }
        allowmod = 0;
        request = new XMLHttpRequest();
        request.onreadystatechange = function () {
        var DONE = this.DONE || 4;
        if (this.readyState === DONE){
        var index;
        parsed = JSON.parse(request.responseText);
        console.log(parsed);
        if(parsed.trouble === 1)
        {
        console.log('TROUBLE === 1');
        switch (parsed.mtext){
        case('db'):
        alert('Проблема с базой данных, повторите позднее');
        allowmod = 1;
        break;
        }}
        else {
          var bookstatus = parseInt(parsed.bookstatus);
          console.log('bookstatus:'+bookstatus);
         if(rem) {
           if (eval("$('#modbook"+bid+"').css('display')") === 'block' )
         {eval("$('#modbook"+bid+"').slideToggle('400',function(){$('#modbook"+bid+"').remove();})");} 
          eval("$('#book"+bid+"').fadeOut('400',function(){$('#book"+bid+"').remove();});");
          if(bookstatus===1)
        {newbooks--;
        $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadyb'> | "+readbooks+"</span>");
        }
        else {
          readbooks--;
          $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadyb'> | "+readbooks+"</span>");
        }
         }
         else {
          if (eval("$('#modbook"+bid+"').css('display')") === 'block' )
         {eval("$('#modbook"+bid+"').slideToggle();");}
           eval("$('#readbutton"+bid+"').remove();");
           eval("$('#heading"+bid+"').removeClass('yet').addClass('alreadyb');");
           newbooks--;
           readbooks++;
         $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadyb'> | "+readbooks+"</span>");
         }
         allowmod=1;
        }
        }
        };
        request.open('POST', '/modbook', true);
        request.setRequestHeader('X-Requested-With', 'XMLHttpRequest');  // Tells server that this call is made for ajax purposes.
        request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        //Most libraries like jQuery/Prototype/Dojo do this
        request.send("rem="+rem+"&uid=#{uid}&bid="+bid);
        }
        //End of remove/change status function
           
