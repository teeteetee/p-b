doctype html(lang='en')
head
    meta(charset='utf-8')
    |     
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    |     
    meta(name='viewport', content='width=device-width, initial-scale=1')
    |     
    title P&B
    // Bootstrap
    link(href='/bootstrap/css/bootstrap.min.css', rel='stylesheet')
    // HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries
    // WARNING: Respond.js doesn't work if you view the page via file://
    //if lt IE 9
      script(src='https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js')
      |       
      script(src='https://oss.maxcdn.com/respond/1.4.2/respond.min.js')     
    style.
      @font-face {
      font-family:roboto-thin;
      src:url('/bootstrap/fonts/Roboto-Thin.ttf');
      }
      @font-face {
      font-family:high;
      src: url('/bootstrap/fonts/Attentica.ttf');
      }
      body {
      min-width:768px;
      font-family:roboto-thin;
      background-color:#333;
      }
      .navbar-nav {
       float: right;
      }
      h1{
        font-family:roboto-thin;
      }
      h2{
        font-family:roboto-thin;
      }
      h3{
        font-family:roboto-thin;
      }
      h4{
        font-family:roboto-thin;
      }
      h5{
        font-family:roboto-thin;
      }
      h6{
        font-family:roboto-thin;
      }
      btn {
        font-family:roboto-thin;
      }
      #showLeft:hover {
       color:white !important;
      }
      .cbp-spmenu {
        background: #1d1d1d;
        position: fixed;
      }
      
      .cbp-spmenu h3 {
        text-align:center;
        color: springgreen;
        font-size: 1.9em;
        padding: 20px;
        padding-top:27px;
        margin: 0;
        font-weight: 300;
      }
      
      .cbp-spmenu a {
        display: block;
        color: white;
        font-size: 1.1em;
        font-weight: 300;
      }
      
      .cbp-spmenu a:hover {
        background: #333;
        text-decoration:none !important;
      }
      
      .cbp-spmenu a:active {
        background: #afdefa;
        color: #47a3da;
      }
      .cbp-spmenu-vertical {
        width: 240px;
        margin-left:74px;
        border-left:1px solid #333;
        height: 100%;
        top: 0;
        z-index: 1000;
      }
      
      .cbp-spmenu-vertical a {
        padding: 1em;
      }
      .cbp-spmenu-left {
        left: -240px;
      }
      .cbp-spmenu-left.cbp-spmenu-open {
        left: 0px;
      }
      .cbp-spmenu-push {
        overflow-x: hidden;
        position: relative;
        left: 0;
      }
      
      .cbp-spmenu-push-toright {
        left: 240px;
      }
      
      .cbp-spmenu-push-toleft {
        left: -240px;
      }
      .cbp-spmenu,
      .cbp-spmenu-push {
        -webkit-transition: all 0.3s ease;
        -moz-transition: all 0.3s ease;
        transition: all 0.3s ease;
      }
      .adding{
        box-shadow: inset 0px 0px 10px 1px #181818;
        background-color:#333;
        color:#999;
      }
      .adding input {
        background-color:#333;
        color:white;
      }
      .previous {
        background-color:#EFAF4F !important;
        border:0px;
        border-radius:30px;
      }
      .previous a {
        color:#623B00 !important;
      }
      .next {
        background-color:#565555 !important;
      }
      .next:hover{
        cursor:pointer
      }
      .previous:hover {
        background-color:#DA8403;
        color:white;
        cursor:pointer;
      }
      .previous:active {
        background-color:#DA8403;
        color:white;
      }
      .previous:focus {
        background-color:#DA8403;
        color:white;
      }
      .pager li>a, .pager li>span {
      display: inline-block;
      padding: 5px 14px;
      border: 0px;
      border-radius: 30px;
      }
      .container {
        filter:brightness(1);
        -moz-filter:brightness(1);
        -webkit-filter:brightness(1);
        transition: 0.5s -webkit-filter linear;
        transition: 0.5s -moz-filter linear;
        -webkit-transition: 0.5s -webkit-filter linear;
        -moz-transition: 0.5s -moz-filter linear;
      }
      .navbar-right {
        float: none !important; 
      }
      .navbar-form {
        padding-top: 6 !important;
      }
      input[type="text"], textarea {
      outline: none;
      box-shadow:none !important;
      }
      .datecl {
        color:#333;
      }
      .yet{
       #{yet}
      }
      .alreadybfigures{
      color:rgb(0,0,0);
      opacity:#{spanop};
      }
      .alreadymfigures{
      color:rgb(0,0,0);
      opacity:#{spanop};
      }
      .alreadyb{
      color:rgb(0,0,0);
      opacity:#{headingsspanop};
      }
      .alreadym{
      color:rgb(0,0,0);
      opacity:#{headingsspanop};
      }
      .leftPaddingExtended{
        padding-left:30px;
      }
      textarea:hover, 
      input:hover, 
      textarea:active, 
      input:active, 
      textarea:focus, 
      input:focus,
      button:focus,
      button:active,
      button:hover
      {
          outline:0px !important;
          -webkit-appearance:none;
      }
      label {
        color:white;
      }
      .modpanel .btn{
        margin:10px;
        border-radius:30px;
      }
      .flickb:hover {
        cursor:pointer;
        background-color:#{bhl};
      }
      .flickm:hover {
        cursor:pointer;
        background-color:#{mhl};
      }
      .h1 .small,
       .h1 small,
       .h2 .small,
       .h2 small,
       .h3 .small,
       .h3 small,
       .h4 .small,
       .h4 small,
       .h5 .small,
       .h5 small,
       .h6 .small,
       .h6 small,
       h1 .small,
       h1 small,
       h2 .small,
       h2 small,
       h3 .small,
       h3 small,
       h4 .small,
       h4 small,
       h5 .small,
       h5 small,
       h6 .small,
       h6 small {
      #{small}
      }
      .activemenuitem {
        background-color:#333;
      }
  body
    script.
      if(screen.width < 414){
      window.location='http://m.peopleandbooks.com/';
      }
    nav(class="cbp-spmenu cbp-spmenu-vertical cbp-spmenu-left" id="cbp-spmenu-s1")
      //form(class="navbar-form navbar-right" method='post' action='/srch' id='srch' style='margin-top: 15px;' role="search")
      //  .input-group
      //     input(type="text" class="form-control" name='searchfield' id='searchfield' style='background-color:#222;color:white;border-color:#333;border-top-left-radius:30px;border-bottom-left-radius:30px;') 
      //     span(class='input-group-btn')
      //       button(class="btn btn-default" type="submit" id='searchbutton' style='padding-top:9px;padding-bottom:9px;background-color:#222;color:gray;border-color:#333;border-top-right-radius:30px;border-bottom-right-radius:30px;')
      //         span(class='glyphicon glyphicon-search') 
      a( style='margin-top:20px;' class='activemenuitem') Мои списки
      a(href="/people") Списки друзей
      a(href="/settings") Настройки
      a(href='/about') О проекте
      a(href="/logout") Выйти
      img(class="media-object img-circle" src="/images/pblogov4.png" style='position:absolute;opacity:0.2;left:88px;bottom:20px;width:64px;height:64px;' alt="...")
    .sidebar(style='width:74px;position:fixed;height:100%;background-color:#1d1d1d;text-align:center;z-index:2000;')
      //img(class="media-object img-circle" src="/bootstrap/images/seo.jpg" style='margin-left:5px;margin-top:30px;width:64px;height:64px;' alt="...")
      span(class='glyphicon glyphicon-align-justify' id='showLeft' style='color:#333;font-size:30px;margin-top:20px;')
    .container(style='padding-left:84px;width:100% !important;padding-right:0px;')  
      #heightcheck.row
        .col-xs-6(id='books' style='background-color:#{bh};height:100%;overflow:auto;')
          .row
            .col-xs-12(style='text-align:center;')
              h1(id='booksfigures' style='font-family:high;font-size:200px;font-weight:bold;color:#{bcl};')
              button(class='btn btn-default addbookbutton' style='border-radius:30px;margin-bottom:20px;border:0px;color:white;background-color:#{addbook};' type='button') Добавить книгу
          .row.adding(id='addbook' style='display:none;padding-top:20px;')
            .col-xs-12
              .form-group
                label(for="exampleInputEmail1") Название
                input(type="email" class="form-control" id="booktitle" maxlength='50' placeholder="Crime and punishment")
              .form-group
                label(for="exampleInputEmail1") Автор
                input(type="email" class="form-control" id="author" maxlength='40' placeholder="Dostoevsky F.M.")
              .checkbox
                label
                  input(type="checkbox" id='newbook') 
                  | Уже прочитал
              .checkbox
                label
                  input(type="checkbox" id='star' disabled) 
                  span(class='glyphicon glyphicon-star' style='color:#F2D45C;margin-right:10px;')
                  | Книга понравилась
              .checkbox
                label
                  input(type="checkbox" id='attention') 
                  span(class='glyphicon glyphicon-exclamation-sign' style='color:rgba(255, 255, 255, 0.65);margin-right:10px;')
                  | Прочитать скорее
              nav
                ul(class="pager")
                  li(class="previous")
                    a(style='background-color:#EFAF4F;' id='booksubmitbutton') Add
                  li(class="next")
                    a( class='addbookbutton' style='background-color:#999;') Close
          .row
            #bookinsert.col-xs-12
        .col-xs-6(id='movies' style='background-color:#{mh};height:100%;overflow:auto;')
          .row
            .col-xs-12(style='text-align:center;')
              h1(id='moviesfigures' style='font-family:high;font-size:200px;font-weight:bold;color:#{mcl};')
              button(class='btn btn-default addmoviebutton' style='border-radius:30px;margin-bottom:20px;border:0px;background-color:#{addmovie};color:white;' type='button') Добавить фильм
          .row.adding(id='addmovie' style='display:none;padding-top:20px;')
            .col-xs-12
              .form-group
                label(for="exampleInputEmail1") Название
                input(type="email" class="form-control" id="movietitle" maxlength='50' placeholder="No country for old men")
              .form-group
                label(for="exampleInputEmail1") Год
                input(type="email" class="form-control" id="year" maxlength='4' placeholder="2007")
              .checkbox
                label
                  input(type="checkbox" id='newmovie') 
                  | Уже посмотрел
              .checkbox
                label
                  input(type="checkbox" id='moviestar' disabled) 
                  span(class='glyphicon glyphicon-star' style='color:#F2D45C;margin-right:10px;')
                  | Фильм понравился
              .checkbox
                label
                  input(type="checkbox" id='movieattention') 
                  span(class='glyphicon glyphicon-exclamation-sign' style='color:rgba(255, 255, 255, 0.65);margin-right:10px;')
                  | Посмотреть скорее
              nav
                ul(class="pager")
                  li(class="previous")
                    a(style='background-color:#EFAF4F;' id='moviesubmitbutton') Add
                  li(class="next")
                    a( class='addbookbutton' style='background-color:#999;') Close
          .row
            #movieinsert.col-xs-12
      script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js')
      script(async src='/js/bootstrap.min.js')
      script.
        // Setting user books/movies counters
        var seenmovies = #{seenmovies};
        var newmovies = #{newmovies};
        $('#moviesfigures').append(" "+newmovies+" <span class='alreadymfigures'> | "+seenmovies+"</span>");
        var readbooks = #{readbooks};
        var newbooks = #{newbooks};
        $('#booksfigures').append(" "+newbooks+" <span class='alreadybfigures'> | "+readbooks+"</span>");
        //End of setting counters
        // Populating the columns
        var books = !{books};
        var movies = !{movies};
        if ( books!=0)
        {books.forEach(function(element,index,array){
                       //$('#bookinsert').append("<h4 style='color:white;'>"+element.booktitle+"</h4>");
                       var popbookclass = 'yet';
                       if(element.newbook!=1)
                       {
                        popbookclass='alreadyb';
                       }
                       var popinsert = "<div class='row flickb' onclick='openmodpanel("+element.bid+")' id='book"+element.bid+"'><div class='col-xs-12'><h4 id='heading"+element.bid+"' style='margin-left:40px;' class='"+popbookclass+"'>"+element.booktitle+" <small> "+element.author+" </small>";
                       if(element.star===1&&element.newbook===0){
                         popinsert+="<span style='color:#F2D45C;margin-left:10px;' class='glyphicon glyphicon-star'></span>";
                       }
                       if(element.attention===1&&element.newbook===1){
                         popinsert+="<span style='color:rgba(255, 255, 255, 0.65);margin-left:10px;' class='glyphicon glyphicon-exclamation-sign'></span>";
                       }
                       popinsert+="</h4></div></div><div class='row modpanel' id='modbook"+element.bid+"' style='text-align:center;display:none;background-color:#333;'><div class='col-xs-12'>";
                       if(element.newbook){
                        popinsert+="<button class='btn btn-primary' onclick='modify(0,"+element.bid+")' type='button'>Прочитана</button>";
                       }
                       popinsert+="<button class='btn btn-danger' onclick='modify(1,"+element.bid+")' type='button'>Удалить</button></div</div>";
                       $('#bookinsert').prepend(popinsert);
                    });}
        if(movies!=0)
        {movies.forEach(function(element,index,array){
                       //$('#movieinsert').append("<h4 style='color:white;'>"+element.movietitle+"</h4>");
                       var popmovieclass = 'yet';
                       if(element.newmovie!=1)
                       {
                        popmovieclass='alreadym';
                       }
                       var popinsert = "<div class='row flickm' onclick='openmodmoviepanel("+element.mid+")' id='movie"+element.mid+"'><div class='col-xs-12'><h4 id='movieheading"+element.mid+"' style='margin-left:40px;' class='"+popmovieclass+"'>"+element.movietitle+" <small> "+element.year+" </small>";
                       if(element.star===1&&element.newmovie===0){
                         popinsert+="<span style='color:#F2D45C;margin-left:10px;' class='glyphicon glyphicon-star'></span>";
                       }
                       if(element.attention===1&&element.newmovie===1){
                         popinsert+="<span style='color:rgba(255, 255, 255, 0.65);margin-left:10px;' class='glyphicon glyphicon-exclamation-sign'></span>";
                       }
                       popinsert+="</h4></div></div><div class='row modpanel' id='modmovie"+element.mid+"' style='text-align:center;display:none;background-color:#333;'><div class='col-xs-12'>";
                       if(element.newmovie){
                        popinsert+="<button class='btn btn-primary' onclick='modifymovie(0,"+element.mid+")' type='button'>Просмотрен</button>";
                       }
                       popinsert+="<button class='btn btn-danger' onclick='modifymovie(1,"+element.mid+")' type='button'>Удалить</button></div</div>";
                       $('#movieinsert').prepend(popinsert);
                    });}
        //end of populating the columns
        // Function for opening panel with remove/modify buttons
        function openmodpanel(bid) {
          eval("$('#modbook"+bid+"').slideToggle();");
          eval("$('.modpanel').each(function(){ if($(this).css('display')==='block'&& $(this).attr('id')!='modbook"+bid+"'){$(this).slideToggle(); }});");
        }
        //end of openmodpanel funciton
        // Function for opening panel with remove/modify buttons in movies column
        function openmodmoviepanel(mid) {
          eval("$('#modmovie"+mid+"').slideToggle();");
          eval("$('.modpanel').each(function(){if($(this).css('display')==='block'&& $(this).attr('id')!='modmovie"+mid+"'){$(this).slideToggle();}});");
        }
        //end of openmodpanel funciton for movies column
        // diable/allow cheking checkboxes
        $('#newbook').click(function() {
         if ($(this).is(':checked')) {
           $('#star').attr('checked',false);
           $('#attention').attr('checked',false);
           $('#star').attr('disabled', false);
           $('#attention').attr('disabled', true);
         } else {
           $('#star').attr('checked',false);
           $('#attention').attr('checked',false);
           $('#attention').attr('disabled', false);
           $('#star').attr('disabled', true);
         }
         });
        $('#newmovie').click(function() {
         if ($(this).is(':checked')) {
           $('#moviestar').attr('checked',false);
           $('#movieattention').attr('checked',false);
           $('#moviestar').attr('disabled', false);
           $('#movieattention').attr('disabled', true);
         } else {
           $('#moviestar').attr('checked',false);
           $('#movieattention').attr('checked',false);
           $('#movieattention').attr('disabled', false);
           $('#moviestar').attr('disabled', true);
         }
         });
        // end of disable/allowchecking checkboxes
        // Buttons which open forms for inserting new books/movies
        $('.addbookbutton').click(function(){
          if($('#addmovie').css('display') === 'block')
          {
            $('#addmovie').slideToggle();
          }
          $('#addbook').slideToggle();
          });
        $('.addmoviebutton').click(function(){
          if($('#addbook').css('display') === 'block')
          {
            $('#addbook').slideToggle();
          }
          $('#addmovie').slideToggle();
          });
        //end of addforms open buttons
        // Sliding menu mechanics
        var counter =0;
        var menuLeft = document.getElementById( 'cbp-spmenu-s1' ),
            menuRight = document.getElementById( 'cbp-spmenu-s2' ),
            showLeft = document.getElementById( 'showLeft' ),
            showRight = document.getElementById( 'showRight' ),
            showLeftPush = document.getElementById( 'showLeftPush' ),
            showRightPush = document.getElementById( 'showRightPush' ),
            body = document.body;
        
        showLeft.onclick = function() {
          switch(counter)
            {case(1):
                         counter=0;
                         console.log('increase brightness');
                         $('#showLeft').removeClass('active' );
                         $('#cbp-spmenu-s1').removeClass('cbp-spmenu-open' );
                         $('.container').css({'-webkit-filter':'brightness(1)','-moz-filter':'brightness(1)','filter':'brightness(1)'});
                        break;
                        case(0):
                          counter=1;
                          console.log('decrease brightness');
                          $('#showLeft').addClass('active' );
                          $('#cbp-spmenu-s1').addClass('cbp-spmenu-open' );
                          $('.container').css({'-webkit-filter':'brightness(0.6)','-moz-filter':'brightness(0.6)','filter':'brightness(0.6)'});
                        break;
                      }
        };
        //end of sliding menu mechanics
        $('.container').click(function(){
          if($('#cbp-spmenu-s1').hasClass('cbp-spmenu-open' );){
            $('#showLeft').removeClass('active' );
            $('#cbp-spmenu-s1').removeClass('cbp-spmenu-open' );
            $('.container').css({'-webkit-filter':'brightness(1)','-moz-filter':'brightness(1)','filter':'brightness(1)'});
            $('.bckgrnd').css({'-webkit-filter':'brightness(1)','-moz-filter':'brightness(1)','filter':'brightness(1)'});
          }
          });
        // Book submit mechanics
        var allowsubmit = 1;
        function booksubmit() {
        if(!allowsubmit) {
          alert('Submit is in progress');
          return
        }
        var author = $('#author').val();
        if(!author){
          author='--';
        }
        else {
          if(author.length>50){
            alert('Author name is too long');
            allowsubmit=1;
            return;
          }
        }
        var booktitle = $('#booktitle').val();
        if(!booktitle) {
          alert('Specify book title');
          allowsubmit=1;
          return
        }
        else {
          if(booktitle.length>50){
            alert('Title is too long');
            allowsubmit=1;
            return;
          }
        }
        var uid = #{uid};
        var star;
        var attention;
        var newbook;
        if($('#star').is(':checked')){
          star = 1;
        }
        else {
          star=0;
        }
        if($('#attention').is(':checked')){
          attention = 1;
        }
        else {
          attention=0;
        }
        if($('#newbook').is(':checked')){
          newbook = 0;
        }
        else {
          newbook=1;
        }
        allowsubmit = 0;
        request = new XMLHttpRequest();
        request.onreadystatechange = function () {
        var DONE = this.DONE || 4;
        if (this.readyState === DONE){
        var index;
        parsed = JSON.parse(request.responseText);
        console.log(parsed);
        if(parsed.trouble === 1)
        {
        console.log('TROUBLE === 1');
        alert('Server is down, try again later.')
        allowsubmit=1;
        }
        else {
          var bid = parsed.bid;
          $('#addbook').slideToggle();
         $('#author').val('');
          $('#booktitle').val('');
          $("#checkbox").prop("newbook", false);
          $("#checkbox").prop("star", false);
          $("#checkbox").prop("attention", false);
        var bookclass = 'yet';
        if(newbook===0)
        {
         bookclass='alreadyb';
        }
        var visibleinsert = "<div class='row flickb' onclick='openmodpanel("+bid+")' id='book"+bid+"'><div class='col-xs-12'><h4 id='heading"+bid+"' style='margin-left:40px;' class='"+bookclass+"'>"+booktitle+" <small> "+author+" </small>";
               if(star===1&&newbook===0){
                 visibleinsert+="<span style='color:#F2D45C;margin-left:10px;' class='glyphicon glyphicon-star'></span>";
               }
               if(attention===1&&newbook===1){
                 visibleinsert+="<span style='color:rgba(255, 255, 255, 0.65);margin-left:10px;' class='glyphicon glyphicon-exclamation-sign'></span>";
               }
               visibleinsert+="</h4></div></div>";
               var hiddeninsert="<div class='row modpanel' id='modbook"+bid+"' style='text-align:center;display:none;background-color:#333;'><div class='col-xs-12'>";
               if(newbook){
                hiddeninsert+="<button class='btn btn-primary' id='readbutton"+bid+"' onclick='modify(0,"+bid+")' type='button'>Прочитана</button>";
               }
               hiddeninsert+="<button class='btn btn-danger' onclick='modify(1,"+bid+")' type='button'>Удалить</button></div</div>";
               //$('#bookinsert').prepend(insert);
        //$('#welcome').fadeOut();
        //$('#booksrow').slideToggle();
        if(newbook===1)
        {newbooks++;
        $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadybfigures'> | "+readbooks+"</span>");
        }
        else {
          readbooks++;
          $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadybfigures'> | "+readbooks+"</span>");
        }
        $(hiddeninsert).prependTo('#bookinsert');
        $(visibleinsert).prependTo('#bookinsert').hide().slideDown();
        allowsubmit=1;
        //window.location = 'http://m.peopleandbooks.com';
        }
        }
        };
        request.open('POST', '/addbook', true);
        request.setRequestHeader('X-Requested-With', 'XMLHttpRequest');  // Tells server that this call is made for ajax purposes.
        request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        //Most libraries like jQuery/Prototype/Dojo do this
        console.log("author="+author+"&booktitle="+booktitle+"&star="+star+"&attention="+attention+"&uid="+uid+"&newbook="+newbook);
        request.send("author="+author+"&booktitle="+booktitle+"&star="+star+"&attention="+attention+"&uid="+uid+"&newbook="+newbook);
        }
        //End of book submit mechanics
        $('#booksubmitbutton').click(booksubmit);
        // Function for removing or changing the status of the book
        var allowmod = 1;
        function modify(rem,bid) {
         if (!allowmod) {
          return;
         }
        allowmod = 0;
        request = new XMLHttpRequest();
        request.onreadystatechange = function () {
        var DONE = this.DONE || 4;
        if (this.readyState === DONE){
        var index;
        parsed = JSON.parse(request.responseText);
        console.log(parsed);
        if(parsed.trouble === 1)
        {
        console.log('TROUBLE === 1');
        switch (parsed.mtext){
        case('db'):
        alert('Проблема с базой данных, повторите позднее');
        allowmod = 1;
        break;
        }}
        else {
          var bookstatus = parseInt(parsed.bookstatus);
          console.log('bookstatus:'+bookstatus);
         if(rem) {
           if (eval("$('#modbook"+bid+"').css('display')") === 'block' )
         {eval("$('#modbook"+bid+"').slideToggle('400',function(){$('#modbook"+bid+"').remove();})");} 
          eval("$('#book"+bid+"').fadeOut('400',function(){$('#book"+bid+"').remove();});");
          if(bookstatus===1)
        {newbooks--;
        $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadybfigures'> | "+readbooks+"</span>");
        }
        else {
          readbooks--;
          $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadybfigures'> | "+readbooks+"</span>");
        }
         }
         else {
          if (eval("$('#modbook"+bid+"').css('display')") === 'block' )
         {eval("$('#modbook"+bid+"').slideToggle();");}
           eval("$('#readbutton"+bid+"').remove();");
           eval("$('#heading"+bid+"').removeClass('yet').addClass('alreadyb');");
           newbooks--;
           readbooks++;
         $('#booksfigures').empty().append(" "+newbooks+" <span class='alreadybfigures'> | "+readbooks+"</span>");
         }
         allowmod=1;
        }
        }
        };
        request.open('POST', '/modbook', true);
        request.setRequestHeader('X-Requested-With', 'XMLHttpRequest');  // Tells server that this call is made for ajax purposes.
        request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        //Most libraries like jQuery/Prototype/Dojo do this
        request.send("rem="+rem+"&uid=#{uid}&bid="+bid);
        }
        //End of remove/change status function
        // Function for movie submition 
        function moviesubmit() {
        if(!allowsubmit) {
          return
        }
        var year = $('#year').val();
        if(!year){
          year='--';
        }
        var movietitle = $('#movietitle').val();
        if(!movietitle) {
          alert('Specify movie title');
          allowsubmit=1;
          return
        }
        var uid = #{uid};
        var moviestar;
        var movieattention;
        var newmovie;
        if($('#moviestar').is(':checked')){
          moviestar = 1;
        }
        else {
          moviestar=0;
        }
        if($('#movieattention').is(':checked')){
          movieattention = 1;
        }
        else {
          movieattention=0;
        }
        if($('#newmovie').is(':checked')){
          newmovie = 0;
        }
        else {
          newmovie=1;
        }
        allowsubmit = 0;
        request = new XMLHttpRequest();
        request.onreadystatechange = function () {
        var DONE = this.DONE || 4;
        if (this.readyState === DONE){
        var index;
        parsed = JSON.parse(request.responseText);
        console.log(parsed);
        if(parsed.trouble === 1)
        {
        console.log('TROUBLE === 1');
        alert('Server is down, try again later.')
        allowsubmit=1;
        }
        else {
          var mid = parsed.mid;
          $('#addmovie').slideToggle();
         $('#year').val('');
          $('#movietitle').val('');
          $("#checkbox").prop("newmovie", false);
          $("#checkbox").prop("moviestar", false);
          $("#checkbox").prop("movieattention", false);
        var movieclass = 'yet';
        if(newmovie===0)
        {
         movieclass='alreadym';
        }
        var visibleinsert = "<div class='row flickm' onclick='openmodmoviepanel("+mid+")' id='movie"+mid+"'><div class='col-xs-12'><h4 id='movieheading"+mid+"' style='margin-left:40px;' class='"+movieclass+"'>"+movietitle+" <small> "+year+" </small>";
               if(moviestar===1&&newmovie===0){
                 visibleinsert+="<span style='color:#F2D45C;margin-left:10px;' class='glyphicon glyphicon-moviestar'></span>";
               }
               if(movieattention===1&&newmovie===1){
                 visibleinsert+="<span style='color:rgba(255, 255, 255, 0.65);margin-left:10px;' class='glyphicon glyphicon-exclamation-sign'></span>";
               }
                visibleinsert+="</h4></div></div>";
                var hiddeninsert =" <div class='row modpanel' id='modmovie"+mid+"' style='text-align:center;display:none;background-color:#333;'><div class='col-xs-12'>";
               if(newmovie){
                hiddeninsert+="<button class='btn btn-primary' id='watchbutton"+mid+"' onclick='modifymovie(0,"+mid+")' type='button'>Просмотрен</button>";
               }
               hiddeninsert+="<button class='btn btn-danger' onclick='modifymovie(1,"+mid+")' type='button'>Удалить</button></div</div>";
               //$('#movieinsert').prepend(insert);
        //$('#welcome').fadeOut();
        //$('#moviesrow').slideToggle();
        if(newmovie===1)
        {newmovies++;
        $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadymfigures'> | "+seenmovies+"</span>");
        }
        else {
          seenmovies++;
          $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadymfigures'> | "+seenmovies+"</span>");
        }
        $(hiddeninsert).prependTo('#movieinsert');
        $(visibleinsert).prependTo('#movieinsert').hide().slideDown();
        allowsubmit=1;
        //window.location = 'http://m.peopleandmovies.com';
        }
        }
        };
        request.open('POST', '/addmovie', true);
        request.setRequestHeader('X-Requested-With', 'XMLHttpRequest');  // Tells server that this call is made for ajax purposes.
        request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        //Most libraries like jQuery/Prototype/Dojo do this
        console.log("year="+year+"&movietitle="+movietitle+"&moviestar="+moviestar+"&movieattention="+movieattention+"&uid="+uid+"&newmovie="+newmovie);
        request.send("year="+year+"&movietitle="+movietitle+"&moviestar="+moviestar+"&movieattention="+movieattention+"&uid="+uid+"&newmovie="+newmovie);
        }
        //End of movie submit mechanics
        $('#moviesubmitbutton').click(moviesubmit);
        // Function for removing or changing the status of the movie
        var allowmod = 1;
        function modifymovie(rem,mid) {
         if (!allowmod) {
          return;
         }
        allowmod = 0;
        request = new XMLHttpRequest();
        request.onreadystatechange = function () {
        var DONE = this.DONE || 4;
        if (this.readyState === DONE){
        var index;
        parsed = JSON.parse(request.responseText);
        console.log(parsed);
        if(parsed.trouble === 1)
        {
        console.log('TROUBLE === 1');
        switch (parsed.mtext){
        case('db'):
        alert('Проблема с базой данных, повторите позднее');
        allowmod = 1;
        break;
        }}
        else {
          var moviestatus = parseInt(parsed.moviestatus);
          console.log('moviestatus:'+moviestatus);
         if(rem) {
           if (eval("$('#modmovie"+mid+"').css('display')") === 'block' )
         {eval("$('#modmovie"+mid+"').slideToggle('400',function(){$('#modmovie"+mid+"').remove();})");} 
          eval("$('#movie"+mid+"').fadeOut('400',function(){$('#movie"+mid+"').remove();});");
          if(moviestatus===1)
        {newmovies--;
        $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadymfigures'> | "+seenmovies+"</span>");
        }
        else {
          seenmovies--;
          $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadymfigures'> | "+seenmovies+"</span>");
        }
         }
         else {
          if (eval("$('#modmovie"+mid+"').css('display')") === 'block' )
         {eval("$('#modmovie"+mid+"').slideToggle();");}
           eval("$('#watchbutton"+mid+"').remove();");
           eval("$('#movieheading"+mid+"').removeClass('yet').addClass('alreadym');");
           newmovies--;
           seenmovies++;
         $('#moviesfigures').empty().append(" "+newmovies+" <span class='alreadymfigures'> | "+seenmovies+"</span>");
         }
         allowmod=1;
        }
        }
        };
        request.open('POST', '/modmovie', true);
        request.setRequestHeader('X-Requested-With', 'XMLHttpRequest');  // Tells server that this call is made for ajax purposes.
        request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        //Most libraries like jQuery/Prototype/Dojo do this
        request.send("rem="+rem+"&uid=#{uid}&mid="+mid);
        }
        //End of remove/change status of the movie function
      